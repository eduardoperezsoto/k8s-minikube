apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
spec:
  selfSigned: {}

# Para producción con Let's Encrypt, sustituir por:
# apiVersion: cert-manager.io/v1
# kind: ClusterIssuer
# metadata:
#   name: letsencrypt-issuer
# spec:
#   acme:
#     server: https://acme-v02.api.letsencrypt.org/directory
#     email: tu@email.com
#     privateKeySecretRef:
#       name: letsencrypt-issuer-key
#     solvers:
#     - http01:
#         ingress:
#           ingressClassName: apisix
