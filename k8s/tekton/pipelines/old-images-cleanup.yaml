apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: old-images-cleanup
  namespace: ci
spec:
  description: >
    Scheduled cleanup of old Harbor images. Keeps the currently deployed
    version and the previous one (rollback target). Visible in Tekton Dashboard.
  tasks:
    - name: cleanup
      taskRef:
        kind: Task
        name: old-images-cleanup
